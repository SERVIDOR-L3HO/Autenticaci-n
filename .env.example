# Ejemplo de archivo .env para desarrollo local
# Copia este archivo como .env y ajusta los valores

# Secret key para Flask (genera una aleatoria en producción)
SECRET_KEY=tu-secret-key-super-segura-aqui

# URL de la base de datos PostgreSQL
# Formato: postgresql://usuario:contraseña@host:puerto/nombre_bd
DATABASE_URL=postgresql://usuario:contraseña@localhost:5432/auth_db

# Entorno (development o production)
ENVIRONMENT=development

# Orígenes CORS permitidos (separados por coma)
# IMPORTANTE: Usa orígenes específicos en producción, NO uses '*'
# Si usas '*', las cookies de sesión no funcionarán en navegadores
# Ejemplos: http://localhost:3000,https://tu-app.com,https://www.tu-app.com
CORS_ORIGINS=http://localhost:3000,http://localhost:5000
